spring:
  application:
    name: SecureTicketingReservationApi
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/ticketingdb}
    username: ${DB_USER:ticketing}
    password: ${DB_PASS:ticketing123}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
  cache:
    type: redis
    redis:
        time-to-live: 300000

server:
  port: 8080

app:
  jwt:
    secret: ${JWT_SECRET:ThisIsAVeryLongSecretKeyForJWTSigningThatShouldBeAtLeast512BitsLong!!}
    access-token-expiry-ms: 900000
    refresh-token-expiry-ms: 604800000

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when-authorized

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
  api-docs:
    path: /api-docs

resilience4j:
  ratelimiter:
     instances:
       loginRateLimit:
         limit-for-period: 100
         limit-refresh-period: PT1M
         timeout-duration: PT0S
         
logging:
  level:
    io.github.resilience4j: DEBUG
            
            